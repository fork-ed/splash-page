{
  "name": "forked-splash",
  "version": "0.0.0",
  "description": "A splash page for displaying the functionality of the ForkEd project",
  "author": "Kenni Bawden",
  "dependencies": {
    "d3": "^3.4.8"
  },
  "dev-dependencies": {},
  "scripts": {
    "dev-start": "beefy js/splash.js --live && catw -c 'lessc -' './style/*.less' -o './css/splash.css'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "beefy": "^2.0.2",
    "browserify": "^4.2.0",
    "catw": "^0.2.0",
    "less": "^1.7.3"
  }
}
